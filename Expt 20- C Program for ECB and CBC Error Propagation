#include <stdio.h>

int main()
{
    int P[4] = {10, 20, 30, 40};
    int C[4] = {15, 25, 35, 45};

    printf("Original Ciphertext:\n");

    for(int i = 0; i < 4; i++)
        printf("C%d = %d\n", i + 1, C[i]);

    printf("\nIf C1 has an error in CBC mode:\n");
    printf("P1 = Corrupted\n");
    printf("P2 = Corrupted\n");
    printf("P3 = Not affected\n");
    printf("P4 = Not affected\n");

    return 0;
}
